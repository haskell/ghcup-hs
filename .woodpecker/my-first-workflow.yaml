when:
  - platform: freebsd/amd64
  - event: push
  - event: manual

skip_clone: true

steps:
  - name: build
    image: vm1
    commands:
      - echo "This is the build step"
    backend_options:
      libvirt:
        ssh_config:
          username: root
          password: root
