when:
  - platform: freebsd/amd64
  - event: push
  - event: manual

skip_clone: true

steps:
  - name: build
    image: vm1
    commands:
      - pwd
      - ls -lah
      - echo "This is the build step"
    backend_options:
      libvirt:
        shared_disk:
          label: shareddata
          xml: |
            <disk type='file' device='disk'>
              <driver name='qemu' type='raw'/>
              <source file='/var/lib/libvirt/images/shared.img'/>
              <target dev='vdb' bus='virtio'/>
            </disk>
        ssh_config:
          username: root
          password: root
