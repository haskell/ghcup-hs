when:
  - platform: freebsd/amd64
  - event: push

steps:
  - name: build
    image: vm1
    commands:
      - echo "This is the build step"
      - ls -lah
    auth_config:
      username: root
      password: root
